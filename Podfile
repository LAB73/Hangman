# Uncomment the next line to define a global platform for your project
# platform :ios, '9.0'

target 'Hangman' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

	pod 'Firebase/Core'
	pod 'Firebase/Firestore'

  # Pods for Hangman

  target 'HangmanTests' do
    inherit! :search_paths
    # Pods for testing
  end

  target 'HangmanUITests' do
    inherit! :search_paths
    # Pods for testing
  end

end

target 'Hangman WatchKit App' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Hangman WatchKit App

end

target 'Hangman WatchKit Extension' do
  # Comment the next line if you're not using Swift and don't want to use dynamic frameworks
  use_frameworks!

  # Pods for Hangman WatchKit Extension

end
